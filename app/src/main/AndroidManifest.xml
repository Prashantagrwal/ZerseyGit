<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.zersey">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/e"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"

            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"
            >

        </activity>

        <activity android:name=".Event.NewEvent"
            android:label="Create Event"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".Event.Comment"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".Session.LoginWithGoogle"
            android:label="Sign In"
            />
        <activity android:name=".Event.YourEvent"
            android:label="Your Event"
            ></activity>
        <activity android:name=".Event.AddComment"
            android:label="Add Comment"/>
    </application>

</manifest>